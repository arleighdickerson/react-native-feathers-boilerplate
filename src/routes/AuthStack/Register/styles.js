import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  screenContainer: {
    alignItems:     'center',
    justifyContent: 'center',
    flex:           1,
  },

  button: {
    borderRadius: 50,
    width:        200,
    height:       35,
  },
});
