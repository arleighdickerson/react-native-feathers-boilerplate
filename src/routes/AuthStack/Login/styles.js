import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  screenContainer: {
    flex: 1,
  },

  copyContainer: {
    flex:           1,
    alignItems:     'center',
    justifyContent: 'center',
  },

  formContainer: {
    alignItems: 'center',
    padding:    25,
    flex:       1.25,
  },

  button: {
    borderRadius: 50,
    width:        200,
    height:       35,

  },
});
