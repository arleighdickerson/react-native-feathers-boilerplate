import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  defaultContainer: {
    marginBottom: 20,
  },

  defaultError: {
    position: 'absolute',
    bottom:   -2,
  },
});
